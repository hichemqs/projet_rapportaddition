<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_print_task_report" model="ir.actions.server">
        <field name="name">Print Task Report</field>
        <!-- IMPORTANT -->
        <field name="model_id" ref="project.model_project_task" />
        <!-- POUR APPARAITRE DANS ACTION MENU -->
        <field name="binding_model_id" ref="project.model_project_task" />
        <field name="binding_view_types">form</field>
        <field name="state">code</field>
        <field name="code">action = env.ref('projet_rapportaddition.action_report_task').report_action(record)</field>
    </record>
</odoo>